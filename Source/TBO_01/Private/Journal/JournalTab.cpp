
#include "Journal/JournalTab.h"
#include "Journal/ActionWidget.h"

/*FReply UJournalTab::NativeOnPreviewKeyDown(const FGeometry& InGeometry, const FKeyEvent& InKeyEvent)
{
	const FKey Key = InKeyEvent.GetKey();
	if (Key == LeftTab -> KeyboardKey || Key == LeftTab ->GamepadKey)
	{
		LeftTab -> StartAnimation();
		return FReply::Handled();
	}
	if (Key == RightTab -> KeyboardKey || Key == RightTab ->GamepadKey)
	{
		RightTab -> StartAnimation();
		return FReply::Handled();
	}
	return Super::NativeOnPreviewKeyDown(InGeometry, InKeyEvent);
}*/
